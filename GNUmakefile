# nymphabet (Reason version)
# GNUmakefile
# Keith Waclena <http://www.lib.uchicago.edu/keith/>

BIN = $(HOME)/node_modules/.bin
export PATH := $(BIN):$(PATH)

all::
	env PATH=$(PATH) npm run build

clean::
	env PATH=$(PATH) npm run clean


